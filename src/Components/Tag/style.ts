import styled from '../../theme'

export const Tag = styled.span`
  border-radius: 3px;
  display: inline-box;
  margin: 5px 0;
  padding: 0 5px;
  background-color: ${props => props.theme.color.pcLightYellow};
`
